@page "/signIn"

<EditForm Model="@User" OnValidSubmit="SignInUser">
    <DataAnnotationsValidator />
    <!-- Email input -->
    <div class="form-outline mb-4">
        
        <input type="email" id="emailAddress" class="form-control" @bind="@User.EmailAddress" />
        <label class="form-label" for="emailAddress">البريد الاكتروني</label>
    </div>

    <!-- Password input -->
    <div class="form-outline mb-4">
        <input type="password" id="password" class="form-control" @bind="@User.Password" />
        <label class="form-label" for="password">كلمة المرور</label>
    </div>
    <!-- ToDO: add "remember me" checkbox and "forget your password button" here-->

    <!-- Submit and Register buttons -->
    <div class="text-center">
        <input type="submit" class="btn btn-primary" value="تسجيل الدخول" />
        <p>لست عضوا؟ <a href="Register">قم بتسجيل الان!</a></p>
        <!-- ToDO: sign in with facebook, google etc ... here-->
    </div>
</EditForm>

@code {
                   /*
                   * this code to be added in htmo for remember me and forget password
                   * 
                   <!-- 2 column grid layout for inline styling -->
                   <div class="row mb-4">
                   <div class="col d-flex justify-content-center">
           <!-- Checkbox -->
           <div class="form-check">
               <input class="form-check-input" type="checkbox" value="" id="form2Example31" checked />
       <label class="form-check-label" for="form2Example31"> Remember me </label>
       </div>
       </div>

       <div class="col">
    <!-- Simple link -->
    <a href="#!">Forgot password?</a>
        </div>
        </div>
        */
        /*
            * add sign up with facebook og google using this ui

            <p>or sign up with:</p>
            <button type="button" class="btn btn-link btn-floating mx-1">
        <i class="fab fa-facebook-f"></i>
        </button>

        <button type="button" class="btn btn-link btn-floating mx-1">
        <i class="fab fa-google"></i>
        </button>

        <button type="button" class="btn btn-link btn-floating mx-1">
            <i class="fab fa-twitter"></i>
        </button>

        <button type="button" class="btn btn-link btn-floating mx-1">
            <i class="fab fa-github"></i>
    </button>
    */
    private UserLogin User = new UserLogin();

    private void SignInUser()
    {
        throw new NotImplementedException();
    }
}
